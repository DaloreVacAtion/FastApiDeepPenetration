1) Почему этот код "query = select(User).where(User.username == username)" возвращал None, хотя аналогичный, только
через text(f'сырой запрос') возвращает корректного пользователя?
2) При обновлении пользователя (api/v1/touter.py 40 строка), возвращает не обновленного пользователя, а ошибку, не могу понять, что не так